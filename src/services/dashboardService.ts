/**
 * Dashboard aggregation — Single Source of Truth for Data Widgets.
 * Sprint 1: placeholder only. Wire DB / API in later sprints.
 */
export async function getDashboardData() {
  return {
    todayTasks: [],
    passportSummary: {},
    homeworkSummary: {},
    remainingStudents: {},
  };
}
